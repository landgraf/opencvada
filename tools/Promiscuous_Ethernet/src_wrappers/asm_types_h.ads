with Interfaces.C; use Interfaces.C;

package asm_types_h is

   subtype umode_t is unsigned_short;  -- /usr/include/asm/types.h:8:24

  -- * These aren't exported outside the kernel to avoid name space clashes
  --  

end asm_types_h;
